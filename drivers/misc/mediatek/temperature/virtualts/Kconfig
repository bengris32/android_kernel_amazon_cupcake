
config MTK_VIRTUAL_TS
	bool "virtual temperature sensor for MediaTek package"
	default n
	help
	It support different type sensor
	in this platform. If this option
	is set, it will support humidity
	by virtual temperature sensor.
